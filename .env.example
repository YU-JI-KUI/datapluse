# Datapulse 环境变量配置模板
# 复制为 .env 并填入真实值：cp .env.example .env
# .env 已加入 .gitignore，不会提交到代码库

# 运行环境标识（决定日志格式和 CyberArk 是否启用）可选：dev stg prod
APP_ENV=dev

# ── PostgreSQL 数据库（STG 实例）────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=datapulse
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_PASSWORD

# ── JWT 签名密钥 ────────────────────────────────────────────────────────────────
# 生成：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=datapulse

# ── 本地向量文件根目录（可选，默认 ./nas） ────────────────────────────────────
# 存放 FAISS 索引，需要有读写权限
STORAGE_BASE_PATH=./nas

# ── Embedding 模型路径 ──────────────────────────
EMBEDDING_MODEL_PATH=./models/xiaobu-embedding-v2

# ── 日志配置 可选────────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_DIR=/ark-nav/datapulse/logs
LOG_ROTATION=time
LOG_BACKUP_COUNT=30
