# 业务分类清单

{intents}

# 本轮对话

- **用户问题**:{question}

- **多轮上下文**(含前文 AI 回答;当前问题若含「第二个 / 那个 / 换一个」等指代,回看上一轮 AI 答确定指向):

{ctx}

- **AI 回答**(原始内容,可能是 JSON 渲染卡 / 含 HTML 标签 / 多层嵌套;只读取对用户可见的语义,忽略标签和无关字段):

  {answer_text}

- **下一轮用户输入**(仅用于评「是否解决」,**禁止**用于判意图 / 分发):{next_user_turn}

- **日志是否已把本条分给本BU**:{dispatched_flag}

# 评测任务

{tasks}

# 输出要求

你是 JSON 生成器。本次回复**有且只有一个 JSON 对象**,第一个字符必须是 `{`、最后一个字符必须是 `}`。
禁止任何解释、前言、后语、markdown 围栏(```)、注释。禁止结尾多余逗号。
字段必须齐全、顺序与下方一致;布尔字段只输出 true/false,枚举字段只从给定取值里选。

按下面的字段及取值要求填写(下方是字段说明,不是要照抄的字面值):

{output_schema}
