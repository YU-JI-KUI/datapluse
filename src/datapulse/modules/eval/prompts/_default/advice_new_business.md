# 维度：新业务分类发现

下面是本次评测中，被判定为「**非本 BU**」的客户问题集合（评测模型认为不属于本 BU 现有任何业务分类）。这些问题游离在现有分类体系之外，可能藏着尚未覆盖的新业务需求。

## Payload 字段说明

下面 `{payload}` 里会给到这些数据（供你分析用，不要原样复述）：

| 字段 | 含义 |
| --- | --- |
| 非本 BU 问题总数 / 去重数 | 被判「非本 BU」的问题条数，及去重后的不同问题个数 |
| 问题清单 | 高频非本 BU 问题（问题, 出现次数），已按频次排序 |

## 数据来源（这些样本是怎么筛出来的）

以上数据来自本次评测逐条落库的明细表 `t_eval_task_row`，筛选条件：
- **只取评测模型打标 `business_type = 非本BU` 的行**——即模型认为该问题不属于本 BU 现有任何业务分类（分类清单之外）。
- 这些问题按原始问题文本去重、按出现次数排序。

{payload}

# 任务

1. 对这些问题做**主题归纳**，找出反复出现、成规模的问题簇。
2. 判断能否从中提炼出**新的业务分类**（尤其是非证券业务方向的高频诉求）。
3. 对每个建议新增的分类，给出**分类名 + 一句话定义 + 代表性问题**，并说明大致规模（多少条问题支撑它）。规模太小、太零散的不要硬提。
