# 维度：新业务分类发现

分析被判定为「**非本 BU**」的客户问题——评测模型认为它们不属于本 BU 现有任何业务分类，游离在现有分类体系之外，可能藏着尚未覆盖的新业务需求。

下面 `<payload>` 标签内是评测数据，含：非本 BU 问题的总数与去重数、高频问题清单（问题+出现次数，已按频次排序）。这些行是评测模型把 business_type 打标为「非本BU」的问题，来自明细表 `t_eval_task_row`。

<payload>
{payload}
</payload>

# 任务

先通读 `<payload>` 里的全部问题，找出规律，再回答：

1. 对这些问题做**主题归纳**，找出反复出现、成规模的问题簇。
2. 判断能否从中提炼出**新的业务分类**（尤其是非证券业务方向的高频诉求）。
3. 对每个建议新增的分类，给出**分类名 + 一句话定义 + 代表性问题**，并说明大致规模（多少条问题支撑它）。规模太小、太零散的不要硬提。
