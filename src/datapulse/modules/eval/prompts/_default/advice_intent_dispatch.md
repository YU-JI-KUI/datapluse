# 维度：「{intent_name}」分类·分发提升

下面是业务分类 **{intent_name}** 在**分发**上的失败样例：该分未分（漏收）或该拒未拒（误收），每条带评测模型的判断依据 `dispatch_reason`。

## Payload 字段说明

下面 `{payload}` 里会给到这些数据（供你分析用，不要原样复述）：

| 字段 | 含义 |
| --- | --- |
| 分类 | 当前正在分析的业务分类名 |
| 分发失败样例 | 该分类的漏收/误收样例，每条含：问题、分发判断依据、场景（该分未分 / 该拒未拒） |

## 数据来源（这些样本是怎么筛出来的）

以上数据来自本次评测逐条落库的明细表 `t_eval_task_row`，筛选条件：
- **业务分类 = {intent_name}**（`j_intent` 折叠到闭集分类后的标签）；
- 且分发场景 `dispatch_scene ∈ {该分未分, 该拒未拒}`——即该分类里发生漏收或误收的行；分发正确的行不计入。

{payload}

# 任务

1. 针对 **{intent_name}** 这一类，指出它的分发主要错在漏收还是误收，典型是哪些问题。
2. 归因：为什么这类问题分发不准（意图定义模糊、与相邻分类混淆、关键词缺失等）。
3. 给出**让这类问题被正确接住 / 正确拒掉**的具体动作（补该分类的意图定义/示例、加区分规则、调阈值等）。
