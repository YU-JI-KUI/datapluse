# 维度：单个业务分类·分发提升

本轮分析的业务分类：<intent>{intent_name}</intent>

分析该分类在**分发**上的失败样例：该分未分（漏收）或该拒未拒（误收）。

下面 `<payload>` 标签内是评测数据，含：该分类的漏收/误收样例，每条带原始问题、分发判断依据、场景（该分未分 / 该拒未拒）。这些行是该分类里分发场景为漏收或误收的行，分发正确的不计入，来自明细表 `t_eval_task_row`。

<payload>
{payload}
</payload>

# 任务

先通读 `<payload>` 里的全部样例，找出规律，再回答：

1. 针对 **{intent_name}** 这一类，指出它的分发主要错在漏收还是误收，典型是哪些问题。
2. 归因：为什么这类问题分发不准（意图定义模糊、与相邻分类混淆、关键词缺失等）。
3. 给出**让这类问题被正确接住 / 正确拒掉**的具体动作（补该分类的意图定义/示例、加区分规则、调阈值等）。
