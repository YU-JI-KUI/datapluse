## 维度 1 · should_dispatch_to_bu(该不该由本BU承接)

判断这个问题**该不该由本 BU 承接**:

- 与本 BU 业务**相关** → `true`(该承接)
- 与本 BU **无关**(他业务 / 闲聊 / 拒识) → `false`(该拒识)

**判断依据**

- ⚠️ 只依据「当前问题 + 多轮上下文(前文)」。
- ⚠️ **绝不能**用「下一轮」反推 —— agent 当时还不知道用户下一轮会说什么。
- 把判断理由写进 `dispatch_reason`。
