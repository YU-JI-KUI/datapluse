# 维度：BU 分发问题诊断（全局）

分析本次评测中问答系统在**分发**上的表现。分发有两类错误：
- **漏收（该分未分）**：评测模型认为该本 BU 承接（should_dispatch=true），但日志实际没分给本 BU（dispatched=false）。
- **误收（该拒未拒）**：评测模型认为不该本 BU 承接（should_dispatch=false），但日志实际分给了本 BU（dispatched=true）。
- 分发正确的行（正常）不在数据里，因为它们没有分发问题。

下面 `<payload>` 标签内是评测数据，含：分发准确率、漏收/误收总数、高频被漏收/误收问题（问题+次数）、代表性样例（问题+AI答案+分发判断依据）。数据来自评测逐条落库的明细表 `t_eval_task_row`。

<payload>
{payload}
</payload>

# 任务

先通读 `<payload>` 里的全部数据，找出规律，再回答：

1. 指出**哪些高频问题**被漏收、哪些被误收（结合数据里的原始问题）。
2. 归因：为什么会漏 / 误收（意图覆盖不全、分发阈值偏高/偏低、拒识规则太宽/太严，还是别的）。
3. 给出**具体的分发调整动作**（补哪类意图词条、调什么规则、加什么拒识），并说明预期能挽回哪些问题。
