# 维度：「{intent_name}」分类·解决率提升

下面是业务分类 **{intent_name}** 里「没有解决」的情况：四类未解决原因（答非所问 / 信息不全 / 事实存疑 / 分发错误）的分布计数，以及失败样例（含原始问题、AI 答案、原因归类）。

## Payload 字段说明

下面 `{payload}` 里会给到这些数据（供你分析用，不要原样复述）：

| 字段 | 含义 |
| --- | --- |
| 分类 | 当前正在分析的业务分类名 |
| 进漏斗 X 条，解决率 | 该分类实际分入本 BU 的样本数及其解决率 |
| 未解决四归因分布 | 答非所问 / 信息不全 / 事实存疑 / 分发错误 各自的条数 |
| 未解决样例 | 该分类的未解决样例，每条含：问题、AI 答案、未解决原因 |

## 数据来源（这些样本是怎么筛出来的）

以上数据来自本次评测逐条落库的明细表 `t_eval_task_row`，筛选条件：
- **业务分类 = {intent_name}**（`j_intent` 折叠到闭集分类后的标签）；
- 且进入本 BU 漏斗（`dispatched_to_bu=true`，即「进漏斗」的分母）；
- **未解决 = `answer_resolved ∈ {no, partial}`**；已解决（`yes`）只计入解决率、不进未解决样例。

{payload}

# 任务

1. 针对 **{intent_name}** 这一类，指出它的解决率主要**败在哪一类原因**。
2. 归因：结合 AI 答案看，问题出在知识库缺内容、答案没答全、还是答错了。
3. 给出**改进这类答案的具体动作**（补哪些标问 / 知识库字段、修正哪类错误答案、是否需要人工兜底），尽量落到可操作的条目。
