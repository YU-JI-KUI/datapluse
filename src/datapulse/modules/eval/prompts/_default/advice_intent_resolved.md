# 维度：单个业务分类·解决率提升

本轮分析的业务分类：<intent>{intent_name}</intent>

分析该分类里「没有解决」的情况：四类未解决原因（答非所问 / 信息不全 / 事实存疑 / 分发错误）的分布，以及失败样例。

下面 `<payload>` 标签内是评测数据，含：该分类进本 BU 漏斗的样本数、解决率、四类未解决原因分布计数、未解决样例（问题+AI答案+未解决原因）。只统计该分类里进本 BU 漏斗（dispatched=true）且未解决（answer_resolved 为 no/partial）的行，来自明细表 `t_eval_task_row`。

<payload>
{payload}
</payload>

# 任务

先通读 `<payload>` 里的全部样例，找出规律，再回答：

1. 针对 **{intent_name}** 这一类，指出它的解决率主要**败在哪一类原因**。
2. 归因：结合 AI 答案看，问题出在知识库缺内容、答案没答全、还是答错了。
3. 给出**改进这类答案的具体动作**（补哪些标问 / 知识库字段、修正哪类错误答案、是否需要人工兜底），尽量落到可操作的条目。
